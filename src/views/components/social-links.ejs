<%
/**
 * Composant réutilisable pour afficher les réseaux sociaux
 * Utilise les données de la table social_links
 * Props: socialLinks (array), location ('hero', 'footer'), user (pour édition)
 */
const socialLocation = typeof location !== 'undefined' ? location : 'footer';
const showEdit = typeof user !== 'undefined' && user;
%>

<% if (typeof socialLinks !== 'undefined' && socialLinks.length > 0) { %>
  <div class="social-links social-links--<%= socialLocation %>">
    <% socialLinks.forEach(link => { %>
      <a 
        href="<%= link.url %>" 
        class="social-link"
        target="_blank" 
        rel="noopener noreferrer"
        aria-label="<%= link.label %> (ouvre dans un nouvel onglet)"
        title="<%= link.label %>"
      >
        <i class="<%= link.icon_name %>"></i>
      </a>
    <% }); %>
  </div>
<% } else { %>
  <p class="text-muted">Aucun réseau social configuré.</p>
<% } %>

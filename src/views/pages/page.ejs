<%
/**
 * Page de rendu (mode visiteur)
 * Utilise le layout page.ejs
 */
%>
<%-
  include('../layouts/page', {
    title,
    description,
    config,
    fonts,
    socialLinks,
    user,
    mode,
    body: include('../components/section-renderer', { config, mode, user, schemas: {} })
  })
%>
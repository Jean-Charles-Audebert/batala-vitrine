<%-
  include('../partials/head', {
    title,
    subtitle: 'Batala Vitrine',
    stylesheets: ['/css/admin.css', '/css/buttons.css']
  })
%>
  <div class="admin-container">
    <%-
      include('../partials/admin-toolbar', {
        title,
        toolbarButtons: [
          { href: '/admin/fonts', class: 'btn-secondary', icon: 'arrow_back', label: 'Annuler' }
        ]
      })
    %>

    <%- include('../partials/alerts') %>

    <div class="alert-box alert-info mb-3">
      <h3 class="mt-0 mb-2" style="color: #1976d2;">💡 Comment utiliser Google Fonts</h3>
      <ol class="mb-0" style="padding-left: 1.5rem; line-height: 1.8;">
        <li>Rendez-vous sur <a href="https://fonts.google.com" target="_blank" rel="noopener" style="color: #1976d2; font-weight: bold;">Google Fonts</a></li>
        <li>Sélectionnez une police (ex: Roboto)</li>
        <li>Choisissez les variantes de poids que vous voulez (400, 700, etc.)</li>
        <li>Cliquez sur "View selected families" (icône panier en haut à droite)</li>
        <li>Copiez le lien <code>&lt;link href="..."&gt;</code> qui apparaît</li>
        <li>Revenez ici et collez-le ci-dessous</li>
      </ol>
    </div>

    <form method="POST" action="<%= formAction %>" class="form-block">
      <div class="form-group">
        <label for="name">Nom de la police *</label>
        <input 
          type="text" 
          id="name" 
          name="name" 
          required 
          placeholder="Roboto"
          value="<%= font ? font.name : '' %>"
        >
        <small class="form-help">Nom affiché dans le sélecteur (ex: "Roboto", "Montserrat Bold")</small>
      </div>

      <div class="form-group">
        <label for="url">URL Google Fonts *</label>
        <input 
          type="url" 
          id="url" 
          name="url" 
          required 
          placeholder="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap"
          value="<%= font ? font.url : '' %>"
        >
        <small class="form-help">
          L'URL complète fournie par Google Fonts. Exemple :<br>
          <code class="text-small">https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap</code>
        </small>
      </div>

      <div class="form-group">
        <label for="font_family">Famille CSS (font-family) *</label>
        <input 
          type="text" 
          id="font_family" 
          name="font_family" 
          required 
          placeholder="'Roboto', sans-serif"
          value="<%= font ? font.font_family : '' %>"
        >
        <small class="form-help">
          La valeur CSS à utiliser. Exemples :<br>
          • <code>'Roboto', sans-serif</code><br>
          • <code>'Montserrat', sans-serif</code><br>
          • <code>'Playfair Display', serif</code>
        </small>
      </div>

      <div class="flex-row gap-md mt-3">
        <button type="submit" class="btn btn-primary flex-1">
          <img src="/icons/save.svg" alt="" class="icon">
          Ajouter la police
        </button>
        <a href="/admin/fonts" class="btn btn-secondary">
          <img src="/icons/cancel.svg" alt="" class="icon">
          Annuler
        </a>
      </div>
    </form>
  </div>
</body>
</html>

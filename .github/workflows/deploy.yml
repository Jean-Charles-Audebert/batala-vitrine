name: Build and Deploy to NAS

on:
  push:
    branches:
      - main
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - '.vscode/**'

env:
  DOCKER_IMAGE: ${{ secrets.DOCKER_USERNAME }}/batala-vitrine
  NODE_VERSION: '24'

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run unit tests
        run: npm test

      - name: Run lint
        run: npm run lint

  build-and-push:
    name: Build Docker Image
    needs: test
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.DOCKER_IMAGE }}
          tags: |
            type=raw,value=latest
            type=sha,prefix={{branch}}-

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=registry,ref=${{ env.DOCKER_IMAGE }}:buildcache
          cache-to: type=registry,ref=${{ env.DOCKER_IMAGE }}:buildcache,mode=max

  # deploy-to-nas:
  #   name: Deploy to NAS
  #   needs: build-and-push
  #   runs-on: ubuntu-latest
  #   
  #   steps:
  #     - name: Deploy via SSH
  #       uses: appleboy/ssh-action@v1.0.3
  #       with:
  #         host: ${{ secrets.NAS_HOST }}
  #         username: ${{ secrets.NAS_USER }}
  #         key: ${{ secrets.NAS_SSH_KEY }}
  #         port: ${{ secrets.NAS_SSH_PORT }}
  #         script: |
  #           cd /volume1/docker/batala-vitrine
  #           ./deploy.sh
  #
  #     - name: Wait for deployment
  #       run: sleep 15
  #
  #     - name: Health check
  #       run: |
  #         for i in {1..10}; do
  #           if curl -f ${{ secrets.NAS_APP_URL }}/health; then
  #             echo "✅ Deployment successful"
  #             exit 0
  #           fi
  #           echo "⏳ Waiting for app to start (attempt $i/10)..."
  #           sleep 5
  #         done
  #         echo "❌ Health check failed"
  #         exit 1
  
  # NOTE: Déploiement SSH désactivé temporairement (problème de PATH Docker sur Synology)
  # Pour déployer : ssh admin@nas puis cd /volume1/docker/batala-vitrine && ./deploy.sh
